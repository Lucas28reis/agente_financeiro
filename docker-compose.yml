version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspaces
    ports:
      - 8888:8888
      - 8501:8501
      - 5000:5000